/**
 * uniapp 内置 SCSS 变量文件
 * 此文件会被自动注入到每个组件的 <style lang="scss"> 中
 * 因此只能包含变量声明与 mixin 定义，不能包含会生成 CSS 输出的规则
 */
/* 引入设计 token 变量（仅变量声明，无 CSS 输出） */
/**
 * 设计 Token - SCSS 变量定义
 * 从 colors_and_type.css 提取并转换为 SCSS 变量
 * 此文件仅包含变量声明，不生成 CSS 输出（可安全被 uni.scss 自动注入）
 */
/* === 暖奶油底色（全局背景） === */
/* === 品牌主色：暖奶油 === */
/* === 咖啡棕系（咖啡主题主色） === */
/* === 美食绿系（美食主题主色） === */
/* === neutral 灰阶 === */
/* === 状态色 === */
/* === 语义别名（指向常用 token） === */
/* === 圆角 === */
/* === 字体 === */
/* === 字号 === */
/* === 字重 === */
/* === 行高 === */
/* === 阴影 === */
/* === 间距（rpx 适配小程序） === */
/* === 动效时长与缓动 === */
/* === 安全区域 === */
/**
 * 二期视觉 Token
 * 仅包含变量声明，供新版页面逐步迁移使用，不直接生成 CSS。
 */
/* 手绘标题字体（猫啃什锦黑，OFL 1.1）
 * 此处只声明字体名；@font-face 本体在 scss/font-maoken.scss，
 * 需要使用的页面自行 @import，避免 base64 数据被重复打进每个页面的 wxss
 * 注意：该字体为单字重（usWeightClass 500），使用处不要再设 font-weight: bold */
/* 引入 mixin 定义（仅 @mixin，无 CSS 输出） */
/**
 * 通用 SCSS Mixin
 * 此文件仅包含 @mixin 定义，不生成 CSS 输出（可安全被 uni.scss 自动注入）
 */
/* === 卡片样式 === */
/* 主题卡片：带主题色渐变背景 */
/* === 按钮 === */
/* 主题主按钮 */
/* 次级按钮 */
/* 禁用按钮 */
/* 圆形图标按钮（如返回按钮、加号按钮） */
/* === 安全区域 === */
/* === 文本省略 === */
/* === flex 工具 === */
/* === 状态徽章 === */
/* === 1px 细边框（小程序适配） === */
/* === 点击反馈 === */
/* === 应用外壳页（页面级布局标准之一） === */
/*
 * 适用：点单页这类「头部/底部固定 + 中间区域内部滚动」的应用外壳页
 * 必须 height:100vh（不能 min-height）+ overflow:hidden，
 * 否则 flex 子项 min-height:auto 会被内容撑高，整个页面变成 body 滚动，
 * 导致头部/底部随页面滚走、内部 scroll-view 的高度等于内容高度而完全无法滚动
 * 搭配 page-shell-body 使用
 */
/* 外壳页的中间滚动区：flex:1 + min-height:0 允许收缩，内部 scroll-view 才能生效 */
/* === uniapp 内置变量映射到设计 token === */
/* 主色调：暖奶油 / 咖啡棕 */
/* 文字色 */
/* 背景色 */
/* 边框色 */
/* 字号 */
/* 间距 */
/* 圆角 */
.page-onboarding.data-v-739199e6 {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background-color: #fff9ed;
  /* 这里刻意不设 overflow: hidden —— 卡片垂直居中后，极小屏上内容若超出容器，
     顶部卡片会被裁掉且无法滚动。裁剪交给下面的 .bg-decor 自己做 */
  padding-bottom: calc(60rpx + env(safe-area-inset-bottom));
}

/* === 背景装饰 === */
.bg-decor.data-v-739199e6 {
  position: absolute;
  /* 用四向声明而非 inset 简写：inset 属较新的逻辑属性，
   * 老版本小程序 WebView 上可能不生效，会导致装饰块掉进文档流 */
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  pointer-events: none;
  z-index: 0;
  /* 装饰色块超出页面边界时在此裁掉，替代页面级 overflow: hidden */
  overflow: hidden;
}
.blob.data-v-739199e6 {
  position: absolute;
  border-radius: 50%;
  opacity: 0.4;
}
.blob.blob-coral.data-v-739199e6 {
  top: -150rpx;
  right: -130rpx;
  width: 420rpx;
  height: 420rpx;
  background-color: #f8c1b4;
}
.blob.blob-leaf.data-v-739199e6 {
  bottom: -140rpx;
  left: -140rpx;
  width: 400rpx;
  height: 400rpx;
  background-color: #dbe8c8;
}

/* === 顶部 === */
.hero.data-v-739199e6 {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-left: 60rpx;
  padding-right: 60rpx;
  padding-bottom: 32rpx;
}
.hero .step-dots.data-v-739199e6 {
  display: flex;
  align-items: center;
  gap: 14rpx;
  margin-bottom: 28rpx;
}
.hero .step-dots .dot.data-v-739199e6 {
  width: 46rpx;
  height: 10rpx;
  border-radius: 999rpx;
  background-color: rgba(118, 85, 64, 0.2);
  transition: background-color 260ms cubic-bezier(0.22, 0.75, 0.24, 1), width 260ms cubic-bezier(0.22, 0.75, 0.24, 1);
}
.hero .step-dots .dot.active.data-v-739199e6 {
  width: 68rpx;
  background-color: #e97a69;
}
.hero .step-dots .dot.done.data-v-739199e6 {
  background-color: rgba(118, 85, 64, 0.42);
}
.hero .hero-title.data-v-739199e6 {
  font-size: 52rpx;
  font-weight: 500;
  letter-spacing: 2rpx;
  color: #624735;
}

/* === 选择卡片列表 === */
.pick-list.data-v-739199e6 {
  position: relative;
  z-index: 1;
  flex: 1;
  display: flex;
  flex-direction: column;
  /* 多余高度不在这里分配，交给首尾两个 .spacer；
   * 不用 justify-content: center 的原因见模板注释 */
  justify-content: flex-start;
  padding: 0 48rpx;
}

/* 弹性留白：按 flex-grow 比例吸收剩余高度。
 * 用比例而非固定 padding 的好处是屏幕变矮时留白自动收窄，不会把卡片挤出屏幕 */
.spacer.data-v-739199e6 {
  flex-grow: 0;
  flex-shrink: 0;
  flex-basis: 0;
}
.spacer.spacer-top.data-v-739199e6 {
  flex-grow: 25;
}
.spacer.spacer-bottom.data-v-739199e6 {
  flex-grow: 75;
}

/* 两张卡片之间的间距（改用 margin 是因为 gap 会在 spacer 与卡片之间也产生间距） */
.pick-card + .pick-card.data-v-739199e6 {
  margin-top: 40rpx;
}

/* 卡片基础：与首页入口卡同一套手绘语言（深棕描边 + 硬投影 + 不规则圆角 + 轻微旋转） */
/* === 选择卡片 ===
 * 外观全部由 CSS 变量描述，「中性 / 选中 / 退后」三态只改变量值。
 * 这样写是为了避免用选择器互相覆盖：早前版本把 transform 写在 .selected 与 .card-xxx 上，
 * 两者特异性相同、靠源码顺序决胜，结果卡片的按下反馈被静默覆盖。
 */
.pick-card.data-v-739199e6 {
  position: relative;
  flex: 0 0 auto;
  /* 不参与压缩，避免小屏上卡片被挤扁 */
  display: flex;
  align-items: center;
  gap: 28rpx;
  padding: 36rpx 32rpx;
  --tilt: 0deg;
  --scale: 1;
  --shift: 0rpx;
  --bg: #f2e9df;
  --bd: #765540;
  --lift: 9rpx 11rpx 0 rgba(98, 71, 53, 0.16);
  --ring: 0 0 0 0 rgba(98, 71, 53, 0);
  /* 性别卡用云存储背景图，身份卡暂无图、沿用底色：
   * 底色保留在图片之下，图片加载期间先显示底色，不会白屏。 */
  background-color: var(--bg);
  background-size: 100% 100%;
  background-repeat: no-repeat;
  background-position: center;
  border: 4rpx solid var(--bd);
  box-shadow: var(--lift), var(--ring);
  transform: translateY(var(--shift)) rotate(var(--tilt)) scale(var(--scale));
  /* === 点击动效：全程单一方向的「浮起」 ===
   *
   * 早前是「按下压扁 → 抬起弹回」的两段式。那个机制注定有顿挫感 —— 运动的
   * **方向在抬起的那一刻反转了**，而这个转折点无论把时长缩短到多少、幅度压到多小，
   * 用户都能感知到「顿一下」。所以继续在原设计上调参数（140→110ms、3rpx→2rpx……）
   * 走不通，必须换机制。
   *
   * 现在只保留一段单向运动：手指抬起、选中态生效的那一刻，卡片沿**同一个方向**
   * 完成「位移 + 放大 + 描边加深 + 投影加长」，四者共用同一条曲线与时长，
   * 整体像被「抬起来」的一次动作，中途没有任何反向。
   *
   * 另一张卡同步单向落下（浮起 → 中性 → 退后），方向同样单调，不产生往复。
   */
  transition: transform 220ms cubic-bezier(0.22, 0.75, 0.24, 1), box-shadow 220ms cubic-bezier(0.22, 0.75, 0.24, 1), background-color 220ms cubic-bezier(0.22, 0.75, 0.24, 1), border-color 220ms cubic-bezier(0.22, 0.75, 0.24, 1);
  /* 入场改为纯淡入（原 slideUp 带位移动画）：除观感更安静外，关键是
   * animation 播放期间会接管 transform，若用户入场后立刻点卡片，
   * 浮起动效会被入场动画压住而「没反应」。淡入不碰 transform，冲突消失。 */
  animation: fadeIn 0.45s cubic-bezier(0.22, 0.75, 0.24, 1) both;
  /* 按下时的唯一反馈：一层极淡的遮罩，**纯色彩、零位移**。
   * 留它是为了按下到抬起之间不至于完全没有响应；
   * 因为它不产生任何位移，也就不会带来「压扁再弹回」的往复感。
   * 放在 ::before 而非 ::after：伪元素是卡片的第一个子元素，位于背景之上、
   * 内容之下，遮罩不会压住头像与文字。 */
  /* === 退后态的淡化遮罩 ===
   * 底色时代靠改 --bg 变淡，现在下层是固定图片，改用「叠加一层页面底色」来淡化：
   * 用 $p2-paper 而不是灰色 —— 让未选中的卡「褪向背景」，而不是「落到阴影里」。
   * 放在 ::after 上，并把四个内容容器抬到 z-index: 1，遮罩就不会压住头像与文字。
   */
}
.pick-card.data-v-739199e6::before {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  border-radius: inherit;
  background-color: rgba(98, 71, 53, 0.12);
  opacity: 0;
  transition: opacity 110ms ease-out;
  pointer-events: none;
}
.pick-card.data-v-739199e6:active::before {
  opacity: 1;
}
.pick-card.data-v-739199e6::after {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  border-radius: inherit;
  background-color: #fff9ed;
  opacity: 0;
  pointer-events: none;
  transition: opacity 220ms cubic-bezier(0.22, 0.75, 0.24, 1);
}
.pick-card.dimmed.data-v-739199e6::after {
  opacity: 0.55;
}

/* === 选中态：底色转饱和 + 描边加深 + 双层投影浮起 ===
 * 用三个维度同时表达，而不是只靠一个小标记：
 *   底色饱和 —— 直觉上最直观的「亮起来」
 *   描边加深 —— 由浅棕转深棕，边界更肯定
 *   投影加远 —— 本项目的手绘语言里卡片本就是「贴在纸面上的贴纸」，加深投影等于浮起半寸
 * 不只用颜色表达，是因为粉与蓝的「更鲜艳」在感知上强度并不一致，且需要考虑色觉差异，
 * 所以同时给出形状信号（上浮 8rpx + 放大 1.5%）。
 */
.pick-card.selected.data-v-739199e6 {
  --bd: #624735;
  --lift: 11rpx 16rpx 0 rgba(98, 71, 53, 0.26);
  --ring: 0 0 0 7rpx rgba(98, 71, 53, 0.18);
  --shift: -8rpx;
  --scale: 1.015;
}

/* === 退后态：本列表已有选择时，未选中的卡降低饱和、贴平、略缩 ===
 * 刻意不用整体 opacity —— 那会连文字一起变淡，白底上的对比度会掉到 3:1 以下不可读。
 * 这里只降底色与描边，文字保持原色。
 */
.pick-card.dimmed.data-v-739199e6 {
  --bd: rgba(118, 85, 64, 0.26);
  --lift: 4rpx 5rpx 0 rgba(98, 71, 53, 0.09);
  --scale: 0.985;
}

/* === 卡片各自的底色与倾斜（分别为 0,1,0 特异性）=== */
.card-female.data-v-739199e6 {
  --bg: #f6c7b8;
  --tilt: -1deg;
  border-radius: 32rpx 44rpx 29rpx 46rpx;
  animation-delay: 0.05s;
}
.card-male.data-v-739199e6 {
  --bg: #c9e3e7;
  --tilt: 1deg;
  border-radius: 44rpx 31rpx 47rpx 28rpx;
  animation-delay: 0.15s;
}
.card-diner.data-v-739199e6 {
  --bg: #f9e8aa;
  --tilt: -1deg;
  border-radius: 32rpx 44rpx 29rpx 46rpx;
  animation-delay: 0.05s;
}
.card-cook.data-v-739199e6 {
  --bg: #dbe8c8;
  --tilt: 1deg;
  border-radius: 44rpx 31rpx 47rpx 28rpx;
  animation-delay: 0.15s;
}

/* === 底色按状态取值（组合选择器为 0,2,0，特异性天然高于上面的类型规则，与顺序无关）=== */
/* 选中：转饱和版 */
.selected.card-female.data-v-739199e6 {
  --bg: #f2ab92;
}
.selected.card-male.data-v-739199e6 {
  --bg: #a3d3da;
}
.selected.card-diner.data-v-739199e6 {
  --bg: #f5d274;
}
.selected.card-cook.data-v-739199e6 {
  --bg: #bfd8a2;
}

/* 退后：转褪淡版（保留色相，仍认得出是哪个选项，只是让位给选中卡） */
.dimmed.card-female.data-v-739199e6 {
  --bg: #f5d5c9;
}
.dimmed.card-male.data-v-739199e6 {
  --bg: #d8eaed;
}
.dimmed.card-diner.data-v-739199e6 {
  --bg: #faefc6;
}
.dimmed.card-cook.data-v-739199e6 {
  --bg: #e7f0da;
}

/* === 性别卡：头像素材圆框 === */
.pick-frame.data-v-739199e6 {
  position: relative;
  z-index: 1;
  /* 抬到按下/退后遮罩之上 */
  position: relative;
  flex-shrink: 0;
  width: 148rpx;
  height: 148rpx;
  border-radius: 50%;
  background-color: #fffef9;
  border: 4rpx solid #765540;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}
.pick-art.data-v-739199e6 {
  width: 100%;
  height: 100%;
}

/* === 身份卡：CSS 手绘人物相框（与首页/旧角色页同一套画法） === */
.mascot-frame.data-v-739199e6 {
  position: relative;
  z-index: 1;
  /* 抬到按下/退后遮罩之上 */
  position: relative;
  flex-shrink: 0;
  width: 148rpx;
  height: 148rpx;
  border-radius: 50%;
  background-color: #fffef9;
  border: 4rpx solid #765540;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.mascot.data-v-739199e6 {
  position: relative;
  width: 120rpx;
  height: 120rpx;
  animation: mascotBob-739199e6 3.2s ease-in-out infinite;
}
.mascot.cheer.data-v-739199e6 {
  animation: cheerJump-739199e6 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
}
@keyframes mascotBob-739199e6 {
0%, 100% {
    transform: translateY(0);
}
50% {
    transform: translateY(-6rpx);
}
}
@keyframes cheerJump-739199e6 {
0% {
    transform: translateY(0) scale(1) rotate(0deg);
}
40% {
    transform: translateY(-18rpx) scale(1.1) rotate(-4deg);
}
70% {
    transform: translateY(2rpx) scale(0.97) rotate(2deg);
}
100% {
    transform: translateY(0) scale(1) rotate(0deg);
}
}
/* 通用五官 */
.face.data-v-739199e6 {
  position: absolute;
  left: 50%;
  bottom: 4rpx;
  transform: translateX(-50%);
  width: 92rpx;
  height: 84rpx;
  border-radius: 48% 48% 50% 50%;
  background-color: #ffe3c2;
}
.eye.data-v-739199e6 {
  position: absolute;
  top: 40rpx;
  width: 12rpx;
  height: 14rpx;
  border-radius: 50%;
  background-color: #4a2c1a;
  animation: blink-739199e6 4.2s ease-in-out infinite;
}
.eye.eye-l.data-v-739199e6 {
  left: 22rpx;
}
.eye.eye-r.data-v-739199e6 {
  right: 22rpx;
}
.eye .spark.data-v-739199e6 {
  position: absolute;
  top: 2rpx;
  left: 2rpx;
  width: 4rpx;
  height: 4rpx;
  border-radius: 50%;
  background-color: #fffef9;
}
@keyframes blink-739199e6 {
0%, 91%, 100% {
    transform: scaleY(1);
}
95% {
    transform: scaleY(0.08);
}
}
.blush.data-v-739199e6 {
  position: absolute;
  top: 56rpx;
  width: 16rpx;
  height: 10rpx;
  border-radius: 50%;
  background-color: rgba(255, 139, 139, 0.65);
}
.blush.blush-l.data-v-739199e6 {
  left: 12rpx;
}
.blush.blush-r.data-v-739199e6 {
  right: 12rpx;
}
.mouth.data-v-739199e6 {
  position: absolute;
  left: 50%;
  top: 52rpx;
  transform: translateX(-50%);
  width: 18rpx;
  height: 12rpx;
  border: 3rpx solid transparent;
  border-bottom-color: #c9553e;
  border-radius: 50%;
}

/* 女孩：咖啡棕双丸子头 + 蝴蝶结 */
.mascot-girl .hair-back.data-v-739199e6 {
  position: absolute;
  left: 50%;
  bottom: 14rpx;
  transform: translateX(-50%);
  width: 106rpx;
  height: 96rpx;
  border-radius: 50% 50% 46% 46%;
  background-color: #7b5638;
}
.mascot-girl .bangs.data-v-739199e6 {
  position: absolute;
  top: -8rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 84rpx;
  height: 34rpx;
  border-radius: 50% 50% 46% 46%;
  background-color: #7b5638;
}
.mascot-girl .bun.data-v-739199e6 {
  position: absolute;
  top: 6rpx;
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  background-color: #7b5638;
  box-shadow: inset -4rpx -4rpx 0 rgba(0, 0, 0, 0.08);
}
.mascot-girl .bun.bun-l.data-v-739199e6 {
  left: 2rpx;
}
.mascot-girl .bun.bun-r.data-v-739199e6 {
  right: 2rpx;
}
.mascot-girl .bow.data-v-739199e6 {
  position: absolute;
  top: 4rpx;
  right: -2rpx;
  width: 26rpx;
  height: 16rpx;
}
.mascot-girl .bow.data-v-739199e6::before, .mascot-girl .bow.data-v-739199e6::after {
  content: "";
  position: absolute;
  top: 0;
  width: 12rpx;
  height: 16rpx;
  background-color: #ff8fab;
}
.mascot-girl .bow.data-v-739199e6::before {
  left: 0;
  border-radius: 8rpx 2rpx 2rpx 8rpx;
  transform: rotate(-14deg);
}
.mascot-girl .bow.data-v-739199e6::after {
  right: 0;
  border-radius: 2rpx 8rpx 8rpx 2rpx;
  transform: rotate(14deg);
}
.mascot-girl .bow .bow-knot.data-v-739199e6 {
  position: absolute;
  left: 50%;
  top: 4rpx;
  transform: translateX(-50%);
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background-color: #f7608a;
  z-index: 1;
}

/* 男孩：白色厨师帽 + 两侧头发 */
.mascot-chef .sidehair.data-v-739199e6 {
  position: absolute;
  top: 26rpx;
  width: 14rpx;
  height: 26rpx;
  background-color: #5c4033;
}
.mascot-chef .sidehair.sidehair-l.data-v-739199e6 {
  left: -4rpx;
  border-radius: 8rpx 0 0 8rpx;
}
.mascot-chef .sidehair.sidehair-r.data-v-739199e6 {
  right: -4rpx;
  border-radius: 0 8rpx 8rpx 0;
}
.mascot-chef .chef-hat.data-v-739199e6 {
  position: absolute;
  left: 50%;
  bottom: 66rpx;
  transform: translateX(-50%);
  width: 96rpx;
  height: 56rpx;
  z-index: 2;
}
.mascot-chef .chef-hat .hat-band.data-v-739199e6 {
  position: absolute;
  left: 50%;
  bottom: 0;
  transform: translateX(-50%);
  width: 88rpx;
  height: 22rpx;
  border-radius: 12rpx;
  background-color: #ffffff;
  box-shadow: 0 3rpx 6rpx rgba(44, 27, 20, 0.12);
}
.mascot-chef .chef-hat .hat-puff.data-v-739199e6 {
  position: absolute;
  border-radius: 50%;
  background-color: #ffffff;
  box-shadow: inset -4rpx -4rpx 0 rgba(44, 27, 20, 0.05);
}
.mascot-chef .chef-hat .hat-puff.puff-l.data-v-739199e6 {
  left: 8rpx;
  bottom: 12rpx;
  width: 34rpx;
  height: 34rpx;
}
.mascot-chef .chef-hat .hat-puff.puff-m.data-v-739199e6 {
  left: 50%;
  bottom: 18rpx;
  transform: translateX(-50%);
  width: 40rpx;
  height: 40rpx;
}
.mascot-chef .chef-hat .hat-puff.puff-r.data-v-739199e6 {
  right: 8rpx;
  bottom: 12rpx;
  width: 34rpx;
  height: 34rpx;
}

/* 相框周围的漂浮小装饰 */
.floatie.data-v-739199e6 {
  position: absolute;
  font-size: 22rpx;
  line-height: 1;
  z-index: 3;
  animation: floatieDrift-739199e6 3.6s ease-in-out infinite;
}
.floatie.floatie-heart.data-v-739199e6 {
  top: 12rpx;
  left: 14rpx;
  color: #f7608a;
}
.floatie.floatie-star.data-v-739199e6 {
  bottom: 16rpx;
  right: 12rpx;
  color: #ffb020;
  animation-delay: -1.8s;
}
.floatie.floatie-leaf.data-v-739199e6 {
  top: 14rpx;
  right: 16rpx;
  color: #4a9e5c;
  animation-delay: -0.9s;
}
@keyframes floatieDrift-739199e6 {
0%, 100% {
    transform: translateY(0) scale(1);
    opacity: 0.75;
}
50% {
    transform: translateY(-8rpx) scale(1.15);
    opacity: 1;
}
}
/* === 卡片文字区 === */
.pick-body.data-v-739199e6 {
  position: relative;
  z-index: 1;
  /* 抬到按下/退后遮罩之上 */
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 8rpx;
}
.pick-body .pick-name.data-v-739199e6 {
  font-size: 40rpx;
  font-weight: 500;
  color: #624735;
}

/* === 底部操作 === */
.footer-actions.data-v-739199e6 {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 56rpx 48rpx 0;
}
.footer-actions .action-row.data-v-739199e6 {
  display: flex;
  align-items: center;
  gap: 20rpx;
  width: 100%;
}
.footer-actions .btn-ghost.data-v-739199e6 {
  flex: 0 0 auto;
  padding: 0 36rpx;
  height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid rgba(118, 85, 64, 0.35);
  border-radius: 48rpx 44rpx 48rpx 46rpx;
  font-size: 28rpx;
  color: #8c725e;
  transition: transform 140ms cubic-bezier(0.22, 0.75, 0.24, 1);
}
.footer-actions .btn-ghost.data-v-739199e6:active {
  transform: scale(0.97);
}
.footer-actions .btn-ghost.disabled.data-v-739199e6 {
  opacity: 0.5;
}
.footer-actions .btn-primary.data-v-739199e6 {
  flex: 1;
  height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #e97a69;
  border: 4rpx solid #765540;
  border-radius: 48rpx 44rpx 48rpx 46rpx;
  box-shadow: 7rpx 8rpx 0 rgba(98, 71, 53, 0.14);
  font-size: 30rpx;
  color: #fffef9;
  transition: transform 140ms cubic-bezier(0.22, 0.75, 0.24, 1), opacity 140ms cubic-bezier(0.22, 0.75, 0.24, 1);
  /* 待命态：未选择任何卡片时略微缩小并降透明度；选中后平滑恢复到实色与原尺寸。
   * 这次"点亮"就是把视线从卡片引到按钮的信号（按钮离卡片较远，不引一下容易被忽略） */
}
.footer-actions .btn-primary.data-v-739199e6:active {
  transform: scale(0.97);
}
.footer-actions .btn-primary.disabled.data-v-739199e6 {
  opacity: 0.45;
  transform: scale(0.97);
}
.footer-actions .skip-link.data-v-739199e6 {
  margin-top: 28rpx;
  font-size: 24rpx;
  color: #8c725e;
  text-decoration: underline;
}
.footer-actions .skip-link.data-v-739199e6:active {
  opacity: 0.7;
}