# 家庭点餐系统｜二期需求文档

> 状态：需求沟通与确认阶段
>
> 更新日期：2026-09-04

## 1. 项目背景

本项目是家庭内部使用的点餐与做饭协作系统，主要场景是家庭成员点餐、另一位家庭成员做饭。项目不对接外部支付系统，不以外卖、电商或商业餐饮平台为目标。

二期的产品方向是：在现有家庭点餐闭环的基础上，持续提升产品体验，并增加独立的家庭菜谱能力。

## 2. 二期总体方向

二期包含两条主线：

1. 持续优化当前版本的 UI、交互、状态反馈、稳定性和已有功能。
2. 新增独立的菜谱功能域，让系统从“只能点单”扩展为“菜谱管理、菜单发布和家庭点单”的完整工具。

当前阶段暂不扩展以下 AI 能力：

- AI 自动生成菜谱
- AI 菜品推荐
- AI 语音助手
- AI 营养分析
- AI 食材识别

## 3. 身份模型

系统有两个可切换的家庭用户身份：

- 干饭人
- 做饭人

两者都是家庭用户，不再使用“普通用户/管理员”的产品概念。

### 3.1 身份选择与切换

- 用户首次登录后进入身份选择页。
- 用户重新登录时进入身份选择页。
- 用户完成选择后，系统记录上次选择的身份。
- 后续自动登录或恢复登录态时，自动进入上次选择的身份，不重复要求选择。
- 进入系统后，用户可以点击头像随时切换“干饭人”和“做饭人”。
- 切换身份后，更新下次自动登录时需要恢复的身份。

### 3.2 身份差异

两个身份使用相同的底部导航和基础页面。

唯一明确的界面差异是：

- 干饭人：浏览菜单和菜谱、进行点单。
- 做饭人：在菜单页和菜谱页额外看到配置入口，例如右上角的配置按钮。

做饭人配置入口分别进入菜单配置和菜谱配置功能。

## 4. 底部导航

两个身份都使用完全一致的四个 Tab：

```text
首页 / 菜单 / 菜谱 / 记录
```

要求：

- 当前“点单”Tab 后续调整为“菜单”Tab。
- 点单能力保留在菜单页中。
- 菜谱作为独立 Tab、独立页面和独立业务区域。
- 当前“管理”Tab 不再作为底部导航项。
- 菜单和菜谱的配置能力通过做饭人模式下的页面配置入口进入。

## 5. 菜谱功能

菜谱不是菜品详情页中的一段附属文字，而是独立的内容和管理功能。

菜谱区域需要承载家庭中会做的每一道菜，并支持为每道菜维护具体做法。

### 5.1 菜谱列表

第一阶段应支持：

- 浏览所有已配置菜谱
- 按咖啡/美食或其他菜谱分类浏览
- 搜索菜谱
- 查看菜谱是否完整配置
- 进入菜谱详情

### 5.2 菜谱详情

菜谱详情至少包含：

- 菜谱名称
- 菜品图片
- 制作时间
- 制作难度
- 适合人数或分量
- 口味选项
- 结构化配料清单
- 制作步骤

菜谱详情页与当前用于点单的菜品详情页相互关联，但不合并为同一个页面。

### 5.3 动态配料规则

配料必须支持结构化配置和条件显示。

示例：芹菜炒肉配置辣度选项：

```text
不辣 → 隐藏或取消辣椒
微辣 → 显示辣椒，例如 1 根
中辣 → 显示辣椒，例如 3 根
```

后续规则需要能够扩展到：

- 不同口味对应不同配料
- 同一配料在不同选项下数量不同
- 可选配料和固定配料
- 食材替换
- 糖、盐、油等调味料用量变化

因此，配料不能只保存为一段纯文本。

## 6. 菜谱优先的业务流程

做饭人配置菜单时，只能从已经配置完成的菜谱中选择，不能直接创建没有菜谱支撑的菜单项。

正常业务链路固定为：

```text
先配置菜谱
→ 从已有菜谱中选择并添加到菜单
→ 将菜谱发布为可点菜单项
→ 干饭人浏览菜单并点单
```

### 6.1 菜谱与菜单的关系

- 菜谱是内容源和制作知识。
- 菜单是菜谱的可点单展示和发布层。
- 菜谱可以存在但暂时不加入菜单。
- 菜谱加入菜单后，还可以独立设置分类、排序、推荐和上架状态。
- 干饭人只能点选已经加入菜单且处于上架状态的内容。

### 6.2 菜单配置

做饭人进入菜单配置后，主要操作是：

1. 查看已有菜谱。
2. 从菜谱中选择内容。
3. 将选中的菜谱加入菜单。
4. 设置菜单分类、排序、推荐和上架状态。

如果没有可添加的菜谱，应引导做饭人先去配置菜谱。

## 7. 公共食材图片素材

二期需要建立一套统一风格的公共食材图片素材库，用于：

- 菜谱配料清单
- 动态配料显示
- 菜谱详情页
- 后续菜谱相关 UI

首批素材包括：

- 辣椒
- 盐
- 生抽
- 青菜
- 其他常用配料

素材后续统一确定：

- 视觉风格
- 画幅和尺寸
- 背景处理
- 命名规范
- 文件格式

当前需求阶段只记录素材需求，待菜谱视觉方案确定后统一生成和接入。

## 8. 数据与权限设计原则

后续实现时需要将以下概念分离：

```text
当前身份/工作模式：干饭人或做饭人
真实家庭成员能力：是否允许配置菜单和菜谱
```

不能继续把不可变的 `admin/orderer` 直接当作用户身份和权限的唯一表达。

菜谱建议作为独立数据域，与菜单发布数据分层：

```text
recipes
├── 菜谱基本信息
├── 配料
├── 口味选项
├── 动态规则
└── 制作步骤

menu_items
├── recipeId
├── 菜单分类
├── 上架状态
├── 推荐状态
└── 排序
```

订单仍需要保存下单时的菜品名称、图片、数量和必要的选项快照，避免菜谱或菜单后续修改影响历史订单。

## 9. 现有功能优化优先级

在新增菜谱功能的同时，持续优化当前系统：

- 点单流程和菜单浏览体验
- 购物车反馈与状态保留
- 订单提交、预约和状态反馈
- 菜品详情与菜谱入口衔接
- 做饭人处理订单的交互
- 菜单与菜谱配置体验
- 加载中、空状态、错误状态和重复点击处理
- 页面层级、视觉一致性和动效连贯性

## 10. 待后续确认的问题

以下事项不在本次需求中擅自确定，后续单独确认：

- 订单的开始制作、标记完成和取餐提醒是否仅做饭人可操作。
- 两个家庭用户是否都拥有菜单/菜谱配置能力，还是需要额外的配置权限。
- 菜谱中的口味选择是否仅用于查看和做饭，还是需要同步进入点单订单。
- 咖啡类内容是否必须配置完整菜谱，还是允许特殊处理。
