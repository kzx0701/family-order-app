# 家庭点餐系统｜二期需求文档

> 状态：需求沟通与确认阶段
>
> 更新日期：2026-09-04

## 1. 项目背景

本项目是家庭内部使用的点餐与做饭协作系统，主要场景是家庭成员点餐、另一位家庭成员做饭。项目不对接外部支付系统，不以外卖、电商或商业餐饮平台为目标。

二期的产品方向是：在现有家庭点餐闭环的基础上，持续提升产品体验，并增加独立的家庭菜谱能力。

“家庭”是系统的核心业务单位。菜单、菜谱、点单和做饭协作都围绕家庭内部场景展开。

## 2. 二期总体方向

二期包含两条主线：

1. 持续优化当前版本的 UI、交互、状态反馈、稳定性和已有功能。
2. 新增独立的菜谱功能域，让系统从“只能点单”扩展为“菜谱管理、菜单发布和家庭点单”的完整工具。

当前阶段暂不扩展以下 AI 能力：

- AI 自动生成菜谱
- AI 菜品推荐
- AI 语音助手
- AI 营养分析
- AI 食材识别

### 2.1 UI 视觉方向

二期 UI 改动幅度较大，整体重新建立视觉语言，基本放弃当前版本的主题风格。

新的视觉方向为：

- 动漫手绘风格
- 清新
- 可爱
- 简约
- 轻松、有家庭生活感

后续需要重新设计全局视觉系统、页面布局、组件样式、插画/食材素材和动效。当前版本的咖啡棕/美食绿主题不再作为二期的设计基线。

## 3. 身份模型

系统有两个可切换的家庭用户身份：

- 干饭人
- 做饭人

两者都是家庭用户，不再使用“普通用户/管理员”的产品概念。

### 3.1 首次登录引导

用户首次登录后不能直接进入小程序首页，必须完成两步引导：

#### 第一步：填写家庭名称

- 输入家庭名称。
- 家庭名称不是必填项。
- 用户可以选择跳过或稍后设置。
- 如果用户未填写，系统默认家庭名称为“我的家庭”。

#### 第二步：选择身份

- 必须选择一个身份。
- 只能二选一：干饭人或做饭人。
- 两步都完成后，才允许进入小程序首页。

### 3.2 身份选择与切换

- 用户首次登录后先完成家庭名称填写，再完成身份选择。
- 用户重新登录时，如果已有完整的家庭信息和身份信息，则直接恢复并进入上次选择的身份。
- 用户重新登录时，如果没有完整的家庭信息和身份信息，则进入家庭名称与身份选择引导。
- 用户完成选择后，系统记录上次选择的身份。
- 后续自动登录或恢复登录态时，自动进入上次选择的身份，不重复要求选择。
- 进入系统后，用户可以点击头像随时切换“干饭人”和“做饭人”。
- 切换身份后，更新下次自动登录时需要恢复的身份。

### 3.3 身份差异

两个身份使用相同的底部导航和基础页面。

唯一明确的界面差异是：

- 干饭人：浏览菜单和菜谱、进行点单。
- 做饭人：在菜单页和菜谱页额外看到配置入口，例如右上角的配置按钮；可以配置菜单和菜谱；可以开始制作、标记完成并发送取餐提醒。

做饭人配置入口分别进入菜单配置和菜谱配置功能。

## 4. 底部导航

两个身份都使用完全一致的四个 Tab：

```text
首页 / 菜单 / 菜谱 / 我的
```

要求：

- 当前“点单”Tab 后续调整为“菜单”Tab。
- 点单能力保留在菜单页中。
- 菜谱作为独立 Tab、独立页面和独立业务区域。
- 当前“记录”Tab 改名并升级为“我的”Tab。
- 点单记录不再作为底部一级 Tab，而是从“我的”页面进入。
- 当前“管理”Tab 不再作为底部导航项。
- 菜单和菜谱的配置能力通过做饭人模式下的页面配置入口进入。

### 4.1 “我的”页面

“我的”页面由两个身份共用，作为个人、身份、家庭和历史记录的统一入口。

页面至少包含：

- 个人信息展示与编辑，例如头像、昵称。
- 当前身份展示：干饭人或做饭人。
- 身份切换入口。
- 当前家庭信息展示。
- 进入点单记录的入口按钮。

点单记录继续保留原有能力，例如按日期查看、刷新、分页、取消和删除订单，但入口调整为“我的”页面中的功能入口。

由于已经有专门的“我的”Tab，首页右上角当前的个人头像入口取消。个人信息、身份展示和身份切换统一收口到“我的”页面，首页不再承担个人中心入口职责。

### 4.2 统一点餐页面

当前点菜和点咖啡页面在二期融合为一个统一的点餐页面。页面内部通过 Tab 切换当前菜单类型：

```text
菜品 / 咖啡
```

统一的是页面结构、浏览方式和交互体验；菜品与咖啡的点单数据仍然完全隔离：

- 菜品和咖啡分别维护独立购物车。
- 在菜品 Tab 加购后切换到咖啡 Tab，底部购物车只显示咖啡购物车。
- 在咖啡 Tab 下单时，只提交当前咖啡购物车，生成独立的咖啡订单。
- 切回菜品 Tab 后，之前加购的菜品及数量恢复显示。
- 两类购物车互不覆盖、互不混合。

### 4.3 首页布局

首页保留两个主要点餐入口：

```text
干饭 / 咖啡
```

两个入口都进入统一点餐页面，但需要自动激活对应的菜单状态：

- 点击“干饭”：进入统一点餐页面并激活“菜品”Tab。
- 点击“咖啡”：进入统一点餐页面并激活“咖啡”Tab。
- 两个入口下方展示最近点单记录列表。
- 两个入口不是跳转到两套独立的点餐页面，而是进入同一个统一点餐页面的对应状态。

## 5. 菜谱功能

菜谱不是菜品详情页中的一段附属文字，而是独立的内容和管理功能。

菜谱区域需要承载家庭中会做的每一道菜，并支持为每道菜维护具体做法。

### 5.1 菜谱列表

第一阶段应支持：

- 浏览所有已配置菜谱
- 按咖啡/美食或其他菜谱分类浏览
- 搜索菜谱
- 查看菜谱是否完整配置
- 进入菜谱详情

### 5.2 菜谱详情

菜谱详情至少包含：

- 菜谱名称
- 菜品图片
- 制作时间
- 制作难度
- 适合人数或分量
- 口味选项
- 结构化配料清单
- 制作步骤

菜谱详情页与当前用于点单的菜品详情页相互关联，但不合并为同一个页面。

### 5.3 动态配料规则

配料必须支持结构化配置和条件显示。

示例：芹菜炒肉配置辣度选项：

```text
不辣 → 隐藏或取消辣椒
微辣 → 显示辣椒，例如 1 根
中辣 → 显示辣椒，例如 3 根
```

后续规则需要能够扩展到：

- 不同口味对应不同配料
- 同一配料在不同选项下数量不同
- 可选配料和固定配料
- 食材替换
- 糖、盐、油等调味料用量变化

因此，配料不能只保存为一段纯文本。

## 6. 菜谱优先的业务流程

做饭人配置菜单时，只能从已经配置完成的菜谱中选择，不能直接创建没有菜谱支撑的菜单项。

正常业务链路固定为：

```text
先配置菜谱
→ 从已有菜谱中选择并添加到菜单
→ 将菜谱发布为可点菜单项
→ 干饭人浏览菜单并点单
```

### 6.1 菜谱与菜单的关系

- 菜谱是内容源和制作知识。
- 菜单是菜谱的可点单展示和发布层。
- 菜谱可以存在但暂时不加入菜单。
- 菜谱加入菜单后，还可以独立设置分类、排序、推荐和上架状态。
- 干饭人只能点选已经加入菜单且处于上架状态的内容。
- 咖啡内容与菜品一样，必须先配置菜谱，之后才能加入菜单并被点单。

### 6.2 菜单配置

做饭人进入菜单配置后，主要操作是：

1. 查看已有菜谱。
2. 从菜谱中选择内容。
3. 将选中的菜谱加入菜单。
4. 设置菜单分类、排序、推荐和上架状态。

如果没有可添加的菜谱，应引导做饭人先去配置菜谱。

## 7. 公共食材图片素材

二期需要建立一套统一风格的公共食材图片素材库，用于：

- 菜谱配料清单
- 动态配料显示
- 菜谱详情页
- 后续菜谱相关 UI

首批素材包括：

- 辣椒
- 盐
- 生抽
- 青菜
- 其他常用配料

素材后续统一确定：

- 视觉风格
- 画幅和尺寸
- 背景处理
- 命名规范
- 文件格式

当前需求阶段只记录素材需求，待菜谱视觉方案确定后统一生成和接入。

## 8. 数据与权限设计原则

后续实现时需要将以下概念分离：

```text
当前身份/工作模式：干饭人或做饭人
真实家庭成员能力：是否允许配置菜单和菜谱
```

不能继续把不可变的 `admin/orderer` 直接当作用户身份和权限的唯一表达。

当前已确认：只有做饭人可以配置菜单和菜谱，也只有做饭人可以开始制作、标记完成和发送取餐提醒。后端接口需要同步执行做饭人身份校验。

菜谱建议作为独立数据域，与菜单发布数据分层：

```text
recipes
├── 菜谱基本信息
├── 配料
├── 口味选项
├── 动态规则
└── 制作步骤

menu_items
├── recipeId
├── 菜单分类
├── 上架状态
├── 推荐状态
└── 排序
```

订单仍需要保存下单时的菜品名称、图片、数量和必要的选项快照，避免菜谱或菜单后续修改影响历史订单。

## 9. 现有功能优化优先级

在新增菜谱功能的同时，持续优化当前系统：

- 点单流程和菜单浏览体验
- 购物车反馈与状态保留
- 订单提交、预约和状态反馈
- 菜品详情与菜谱入口衔接
- “我的”页面、个人信息、身份切换和家庭信息展示
- 全局动漫手绘视觉、组件样式和动效一致性
- 做饭人处理订单的交互
- 菜单与菜谱配置体验
- 加载中、空状态、错误状态和重复点击处理
- 页面层级、视觉一致性和动效连贯性

## 10. 待后续确认的问题

以下事项不在本次需求中擅自确定，后续单独确认：

- 菜谱中的口味选择是否仅用于查看和做饭，还是需要同步进入点单订单。
